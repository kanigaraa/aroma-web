import type { Metadata } from "next";
export const metadata: Metadata = { title: "AROMA | Masuk", description: "Masuk ke AROMA." };
export default function LoginLayout({ children }: { children: React.ReactNode }) { return children; }